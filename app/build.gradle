plugins {
    id 'com.android.application'
}

android {
    namespace 'com.novelgithub.exporter'
    compileSdk 35

    defaultConfig {
        applicationId 'com.novelgithub.exporter'
        minSdk 29
        targetSdk 35
        versionCode 1
        versionName '1.0'
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
